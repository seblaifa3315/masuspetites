SuccessContent from masus-petites-storefront. Use via `window.MasusPetites.SuccessContent` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Default

```jsx
() => (
  <div className="bg-background text-foreground">
    <SuccessContent />
  </div>
)
```
