CartContent from masus-petites-storefront. Use via `window.MasusPetites.CartContent` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Empty

```jsx
() => (
  <div className="bg-background text-foreground">
    <CartContent />
  </div>
)
```
