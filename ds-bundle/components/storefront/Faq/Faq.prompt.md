Faq from masus-petites-storefront. Use via `window.MasusPetites.Faq` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Default

```jsx
() => (
  <div className="bg-background text-foreground">
    <Faq />
  </div>
)
```
