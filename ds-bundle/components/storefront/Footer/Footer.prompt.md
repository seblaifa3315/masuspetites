Footer from masus-petites-storefront. Use via `window.MasusPetites.Footer` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Default

```jsx
() => (
  <div className="bg-background text-foreground">
    <Footer />
  </div>
)
```
