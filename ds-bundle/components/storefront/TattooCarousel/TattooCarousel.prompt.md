TattooCarousel from masus-petites-storefront. Use via `window.MasusPetites.TattooCarousel` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### Default

```jsx
() => <TattooCarousel images={images} />
```
