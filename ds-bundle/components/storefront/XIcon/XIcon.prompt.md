XIcon from masus-petites-storefront. Use via `window.MasusPetites.XIcon` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### IconButton

```jsx
() => (
  <div className="p-2 rounded-lg bg-background hover:bg-accent hover:text-white text-muted transition-colors inline-flex">
    <XIcon className="w-5 h-5" />
  </div>
)
```
